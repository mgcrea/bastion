# Commits that changed formatting and nothing else. Listed here so `git blame`
# attributes lines to whoever wrote them rather than to the reformat.
#
# GitHub reads this file automatically. Locally it needs one setup step, which
# `make blame-setup` does:
#
#     git config blame.ignoreRevsFile .git-blame-ignore-revs

# style: swift-format the Swift half
f1a6279dda5a902d87bf53a2d2c213c4bbfce5e9
